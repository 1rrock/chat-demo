providers = ["node"]

[variables]
NODE_ENV = "production"

[phases.setup]
cmds = ["cd server"]

[phases.install]
cmds = ["cd server && npm ci"]

[phases.build]
cmds = ["cd server && npm run build"]

[phases.start]
cmd = "cd server && npm run start:prod"
