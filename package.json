{
  "name": "ws-chat-client",
  "version": "1.0.0",
  "description": "WebSocket Chat Client",
  "scripts": {
    "install": "cd client && npm install",
    "build": "cd client && npm run build",
    "start": "cd client && npm run start"
  },
  "keywords": ["websocket", "chat", "react", "socket.io"],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
